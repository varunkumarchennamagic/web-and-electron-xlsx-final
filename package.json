{
  "name": "web-and-electron-app-trial-2",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "build": {
    "appId": "your.app.id",
    "productName": "Varun's xlsx to json",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": true,
      "allowToChangeInstallationDirectory": true
    }
  },

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"   
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^24.3.1",
    "electron-builder": "^23.6.0"
  }
}
